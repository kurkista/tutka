# Copy to .env (gitignored) and fill in. Never commit real values.

# AIS live ship data — register a free key at https://aisstream.io (GitHub login).
# Without a key the app still runs; the live vessel layer is just disabled.
AISSTREAM_API_KEY=

# OpenSky Network flight layer (M3) — free account at https://opensky-network.org,
# then create an API client (OAuth2 client credentials) in your account settings.
#OPENSKY_CLIENT_ID=
#OPENSKY_CLIENT_SECRET=

# Shared secret for POST /api/ingest/gdelt (the news-relay GitHub Action).
# Generate with: openssl rand -hex 24 — set the same value as a fly secret and
# as the INGEST_TOKEN repo Actions secret.
#INGEST_TOKEN=

# NASA FIRMS wildfire hotspot signal (domain 6's F component) — free
# self-service key at https://firms.modaps.eosdis.nasa.gov/api/map_key/.
# Without a key the app still runs; the F component is just absent.
#FIRMS_MAP_KEY=

# Fingrid Open Data (power-system-state signal, domain 4) — subscription
# key from the Fingrid developer portal (developer-data.fingrid.fi).
# Without a key the app still runs; this signal is just disabled.
#FINGRID_API_KEY=

# Server
PORT=8080
DB_PATH=./dev.db
